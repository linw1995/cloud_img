# globals
global:
  # python module `secrets.token_urlsafe()`
  JWT_KEY: ZSCmULXCSvFBZp9MGq7iHIqBS017fpk7nFRWsbUXo0Y
# environments
production: &default
  db:
    mysql:
      host: localhost
      port: 3306
      db: cloud_img
      user: root
      password:
      minsize: 10
      maxsize: 100

debug:
  <<: *default

test:
  db:
    mysql:
      host: localhost
      port: 3306
      db: cloud_img_test
      user: root
      password:
      minsize: 10
      maxsize: 100
